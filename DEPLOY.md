# 个人理财工作台 · 部署与验收指南

一款可安装、可长期使用的**多市场真实行情个人理财桌面应用**（Windows x64）。
覆盖 A股 / 港股 / 美股 持仓管理、真实行情、汇率折算、财经资讯、每日复盘、事件日历。

---

## 1. 环境要求

| 项目 | 要求 |
| --- | --- |
| 操作系统 | Windows 10 / 11（x64） |
| 运行依赖 | 安装包已内置 Electron 运行时，无需单独安装 Node |
| 网络 | 需要互联网访问行情与资讯源（腾讯/新浪/东方财富/华尔街见闻/金十 等） |
| 管理员权限 | 安装时若选择「为所有用户安装」需管理员；选「仅当前用户」则不需要 |

---

## 2. 安装（交付物）

1. 进入本仓库 `dist/` 目录，双击 `FinanceWorkbench-Setup-1.0.0.exe`。
2. 安装向导可选择安装目录（默认 `%LOCALAPPDATA%\Programs\FinanceWorkbench`）。
3. 勾选「创建桌面快捷方式」「创建开始菜单快捷方式」。
4. 完成安装后从桌面/开始菜单启动「个人理财工作台」。

> 数据目录：`%APPDATA%\finance-workbench\`
> - `finance-workbench.sqlite` —— 持仓 / 现金 / 设置 / 汇率 / 复盘 / 事件 / 新闻 等全部数据
> - `credentials.enc` —— API Key 密文（DPAPI / AES-256-GCM，绝不明文）
> 数据随应用关闭自动落盘，重开自动恢复，**无需手动备份即可长期使用**。

---

## 3. 首次运行 · 种子数据

首次启动会自动写入：
- 现金账户：10 万美元（USD）
- 示例持仓：美股 ETF **ABCD** 2000 股，成本价 250，投入成本 50 万美元（与需求一致）
- 关注指数：恒生科技指数、上证指数
- 默认行情源优先级：腾讯 → 新浪 → 东方财富（公开源，可能限流）

> 可在「我的持仓」中编辑/删除示例持仓，或新增自己的持仓。

---

## 4. 功能与操作

### 4.1 今日总览（首页）
- 总资产 / 证券资产 / 现金 / 当日盈亏 / 总盈亏 / 收益率（以基准币折算）
- 关注指数实时涨跌（恒生科技、上证指数等）
- 统一红绿涨跌率（上涨/下跌/平 占比条，中国习惯：红涨绿跌）
- 右侧「今日总览」卡片：财经资讯 / 仓位复盘 / 我的持仓 / 事件日历 / 设置，点击直达

### 4.2 我的持仓
- 新建 / 编辑 / 删除持仓；字段含 名称、板块、权重、市值、当日盈亏、总盈亏、成本价、涨跌幅、收益率、市场与交易状态
- 数量推导：填「数量+成本价」为精确口径；仅填「投入成本+成本价」可反推精确数量；仅填「投入成本」则按现价估算并标注「量估算」
- 缺失数据处理：缺成本价 → 总盈亏显示「待录入成本」；缺昨收 → 当日盈亏显示「缺昨收」；缺汇率 → 基准币金额显示「—」并标红，绝不填充模拟值

### 4.3 财经资讯
- 源：华尔街见闻 / 金十数据 / 新浪财经7x24 / 交易所公告(东方财富) / 同花顺
- 每条标注：来源、发布时间(北京时间)、抓取时间、摘要、原文链接、展开正文
- 跨源去重（标题+正文指纹 + 近似判重），同源重复已合并
- 相关性标注：与持仓「直接相关」/「市场相关」/「一般财经」

### 4.4 仓位复盘
- 收盘后自动生成 A股 / 港股 / 美股 三市场复盘日报（弹窗提示）
- 严格区分：**客观数据（真实行情）** / **AI 推测（条件性，已标注）** / **风险提示**
- 不臆造未发生的"事实"，所有客观数字可溯源到当日行情快照

### 4.5 事件日历
- 来源分级：美联储 FOMC（官网，已确认）/ A股新股申购·财报预约（交易所/东财，已确认或预约）/ CPI·非农（BLS 规律推算，**全部标注未确认**并附官网链接）
- 全部时间换算为北京时间，保留原始时区；一周内未确认事件标「待核对」；过期自动归档
- 支持手动添加事件

### 4.6 设置
- 基准币种、刷新间隔、收盘自动复盘开关
- 行情源优先级（拖拽排序；已配置 Key 的付费源自动置顶）
- 新闻源 / 关注指数 勾选
- 汇率：自动源刷新 + **手动录入**（标注「[手动]」及录入时间）
- API Key：Tushare / Finnhub / Twelve Data 加密录入；富途 OpenD 网关地址 + 连通性检测
- 数据路径与存储方式展示（安全存储状态）

---

## 5. 行情源说明（重要）

| 源 | 市场 | 是否需要 Key | 备注 |
| --- | --- | --- | --- |
| 腾讯财经 | A/H/US | 否 | 公开源，可能限流/延迟 |
| 新浪财经 | A/H/US | 否 | 公开源，可能限流/延迟（A股备用） |
| 东方财富 | A/H/US | 否 | 公开源，可能限流/延迟 |
| Tushare | A股 | **需 token** | 配置后优先级自动高于公开源 |
| Finnhub | 美股 | **需 key** | 配置后优先级自动高于公开源 |
| Twelve Data | 美股 | **需 key** | 配置后优先级自动高于公开源 |
| 富途 OpenD | 本地网关 | 地址+端口 | 用于打新/权限校验连通性检测 |

**铁律**：任何行情源失败都**不会伪造价格**。取不到数据的标的会明确显示原因、已尝试的源与建议排查方式，并要求用户补充信息或稍后重试。

---

## 6. 二次开发 / 重新打包

```bash
# 1) 安装依赖（已在中国镜像下验证）
npm install

# 2) 自检（纯 Node，覆盖时段判定/数据库/真实行情/汇率/盈亏公式/OpenD/缺失数据，共 29 项）
npm run selftest

# 3) 开发模式启动（需先 npm install 安装 Electron）
npm start

# 4) 打包 Windows NSIS 安装包（x64）
npm run dist
# 产物：dist/FinanceWorkbench-Setup-1.0.0.exe
```

> 打包依赖 `electron-builder`（devDependencies）。NSIS 配置见 `package.json` 的 `build.nsis`。
> `sql-wasm.wasm` 通过 `extraResources` 随包拷贝，运行时从 `process.resourcesPath` 加载，无需用户干预。

### 目录结构
```
finance-workbench/
├── package.json            # 依赖 / 脚本 / 打包配置
├── electron/
│   ├── main.js             # 主进程：窗口 / IPC / 自动刷新 / 收盘复盘
│   ├── preload.js          # 安全桥接（contextBridge）
│   └── lib/
│       ├── http.js         # 统一 HTTP（GBK/UTF-8、超时、延迟统计）
│       ├── sessions.js     # 交易时段 / 价格类型判定（IANA 时区）
│       ├── secure.js       # API Key 加密存储（DPAPI / AES-256-GCM）
│       ├── store.js        # SQLite 数据层（sql.js / WASM）
│       ├── market.js       # 行情引擎（多源降级，绝不模拟）
│       ├── fx.js           # 汇率模块（ECB/er-api + 手动）
│       ├── portfolio.js    # 组合计算引擎（扩展规则）
│       ├── news.js         # 资讯聚合（去重+相关性）
│       ├── calendar.js     # 事件日历（官方/推算分级）
│       └── review.js       # 每日复盘引擎
├── renderer/               # 原生 HTML/CSS/JS 渲染层（6 大页面）
└── tools/selftest.js       # 自检脚本
```

---

## 7. 故障排查

| 现象 | 原因 / 处理 |
| --- | --- |
| 持仓显示「无行情」 | 代码与市场不匹配（A股6位 / 港股5位 / 美股字母），或行情源临时限流；查看该标的提示的具体原因与已尝试源 |
| 总盈亏显示「待录入成本」 | 该持仓未录入成本价，在「我的持仓」编辑补全即可自动计算 |
| 基准币金额显示「—」标红 | 缺少对应汇率，到「设置 → 汇率」点刷新或手动录入 |
| 资讯/日历为空 | 对应数据源网络不通或被限流，稍后点刷新重试 |
| CPI/非农标「未确认」 | 按 BLS 规律推算，未做官方抓取，需点击来源链接人工核对 |
| 复盘未自动弹出 | 确认「设置 → 收盘后自动复盘」已开启；也可在「仓位复盘」手动生成 |

---

## 8. 验收清单

- [ ] 安装包可正常安装并启动
- [ ] 首页六大指标与关注指数正常显示，红涨绿跌
- [ ] 持仓增/删/改生效，盈亏与权重计算正确
- [ ] 行情刷新、市场切换、价格类型（实时/盘前/盘后/最近收盘）判定准确
- [ ] 汇率折算正确，手动汇率标注清晰
- [ ] 新闻列表去重、相关性、原文链接可用
- [ ] 每日复盘区分客观/推测/风险，三市场齐全
- [ ] 事件日历官方/推算分级、北京时间、未确认标注正确
- [ ] API Key 录入后仅显示掩码，明文不落地
- [ ] 关闭重开数据完整恢复（sqlite 落盘）
- [ ] 随机 ≥3 只真实证券行情可正常拉取
